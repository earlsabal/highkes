<p>
  <% user = User.find(params) %>
  <% if user.username == nil %>
    <% user.username = user.email %>
  <% end %>
  <%= render "profile_pic", :user => user %>    <%= link_to (user.username), "/profile/#{user.id}" %>
</p> 